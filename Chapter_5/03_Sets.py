# sets and dictionaries both use {curly brackets}
# bus sets only contain values instead of key:value pairs

s = {1,4,5,6}
print(type(s))
e = set() # we use this to make empty set coz {} makes empty dict
print(type(e))
# elements cant be repeated in set
x = {1,4,6,7,8,8,8}
print(x)