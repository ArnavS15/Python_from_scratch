a = (1,2,5,6)
print(type(a))
# tuple looks like list but without the " " inside parenthesis bw each element
b = () # is empty tuple ek tareeke se
print(type(b))
c = (1,) # is a tuple with signle element but ,  dena zaruri otherwise will take it as integer type
print(type(c))
# tuple is also immutable like string
